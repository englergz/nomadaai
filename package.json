{
  "name": "nomadaai",
  "private": true,
  "version": "0.1.0",
  "description": "NómadaAI — gestión segura de rutas urbanas (Tumaco). Monorepo web-first.",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev:web": "npm run dev --workspace apps/web",
    "build:web": "npm run build --workspace apps/web"
  }
}
